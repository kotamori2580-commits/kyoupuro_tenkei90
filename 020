/**
 * 難易度（体感）：★★★☆☆
 * 結果：WA
 * 解法：
 * * 【学んだこと・ポイント】
 * ・コンピュータの精度不足で計算できない
計算機上で実数を持ち, 以下のことを行う際には十分に注意しなければならない.

四捨五入
切り捨て
切り上げ
一致判定
大小比較

 * * 【反省・詰まった点】
 * ・
 * * 【計算量】
 * ・O(1)
 */

#include <bits/stdc++.h>
using namespace std;

int main(){
  long long a,b,c;
  cin >> a >> b >> c;
  if((b*log2((double)c))>(log2((double)a))){
    cout << "Yes" << "\n";
  }
  else{
    cout << "No" << "\n";
  }
}
