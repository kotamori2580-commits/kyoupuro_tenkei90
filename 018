/**
 * 難易度（体感）：★★★☆☆
 * 結果：できなかった
 * 解法：考察問題
 * * 【学んだこと・ポイント】
 コードに出てきた型や関数は、AtCoderなどの競技プログラミング（特に幾何問題）で非常に重要になるポイントです。それぞれ解説しますね。

### 1. `long long` と `long double` の違い

一言で言うと、**「整数」か「実数（小数）」か** の違いです。

| 型 | 扱う数 | 特徴 | AtCoderでの使い分け |
| --- | --- | --- | --- |
| **`long long`** | **整数** | 64bit整数。<br>

<br>範囲：約  ～  | **個数、金額、組み合わせ**など。<br>

<br>`int`（最大約 ）では足りないときに必ず使う。 |
| **`long double`** | **実数** | 拡張倍精度浮動小数点数。<br>

<br>`double` よりも桁数が多く、精度が高い。 | **座標、確率、角度**など。<br>

<br>今回の観覧車の問題のように `sin`, `cos`, `sqrt` を使う計算では、誤差で不正解（WA）になるのを防ぐために使う。 |

* **注意点:** `long long` で割り算をすると小数が切り捨てられます（例: `3 / 2` は `1` になる）。小数を扱いたい場合は `long double` や `double` を使います。

---

### 2. `acos` (アークコサイン)

数学でいう ** （逆余弦関数）** です。
「 の値」から「角度 」を求めるときに使います。

* **入力:**  から  までの値
* **出力:**  から  までの角度（ラジアン）

**なぜ `acos(-1.0)` を使ったのか？**
数学的に  なので、逆算すると  となります。
円周率  （3.141592...）を自分で手打ちすると打ち間違いや精度の問題が出るため、**`acos(-1.0)` と書いてコンピュータに計算させるのが、最も正確に  を用意する定石**です。

---

### 3. `atan2` (アークタンジェント2)

数学でいう ** （逆正接関数）の進化版** です。
座標  があるとき、原点からその点への角度（偏角）を求めます。

普通の `atan(y/x)` ではなく、`atan2(y, x)` を使うべき絶対的な理由が2つあります。

1. **ゼロ除算エラーが起きない**
* `atan(y/x)` だと、 のとき（真上や真下のとき）に計算できずエラーになります。
* `atan2(y, x)` は、 でも正しく  や  を返してくれます。


2. **象限（しょうげん）を判別してくれる**
* `atan(y/x)` は、「右上」と「左下」の区別がつきません（どちらも  が正になるため）。
* `atan2(y, x)` は、 の符号を見て、 から  の範囲で**360度正しい方向**を返してくれます。



**まとめると：**
座標から角度を求めたいときは、何も考えずに **`atan2(y, x)`** を使うのが正解です。
（※引数の順番が `y, x` であることに注意してください）

今回の問題は  平面上の計算ではなく、距離と高さの比率から角度を出したかったので、
`atan2(高さ, 水平距離)` として使いました。

---

 * * 【反省・詰まった点】
 * ・知らない知識が多かった
 * * 【計算量】
 * ・O(Q)
 */

#include <bits/stdc++.h>
using namespace std;

long double E[1009];

int main(){
  cout << fixed << setprecision(12);
  long double T,L,X,Y;
  cin >> T >> L >> X >> Y;
  int Q;
  cin >> Q;
  for(int i=1;i<=Q;i++){
    cin >> E[i];
  }
  const long double PI=acos(-1);
  
  for(int i=1;i<=Q;i++){
    long double t = E[i];
    long double p = -L*sin(2*PI*t/T)/2;
    long double q = L/2-L*cos(2*PI*t/T)/2;
    long double W = sqrt(X*X +(Y-p)*(Y-p));
    long double D = q;
    long double theta = atan2(D, W);
    long double ans = 180*theta/PI;
    cout << ans << "\n";
  } 
}
